id: S2-v1
scenario: payment_latency        # injector fault key
params: {warmup_seconds: 30}
expected:
  root_cause_label: "paymentsvc latency spiked, driving checkout timeouts and a high error rate on shopapi"
  root_cause_keywords: [paymentsvc, latency]   # judge fallback (all present, case-insensitive)
  remediation: {tool: restart_service, target_service: paymentsvc}
  escalation_level: APPROVE_ACTION
budgets: {max_llm_calls: 40, max_wall_seconds: 420}
