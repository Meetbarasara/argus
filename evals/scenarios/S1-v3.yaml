id: S1-v3
scenario: redis_down        # injector fault key
params: {warmup_seconds: 10, benign_restart: true}
expected:
  root_cause_label: "shopredis is down, breaking shopapi's redis cache dependency (dep_up[redis]=0)"
  root_cause_keywords: [redis, dependency]   # judge fallback (all present, case-insensitive)
  remediation: {tool: restart_service, target_service: shopredis}
  escalation_level: NOTIFY
budgets: {max_llm_calls: 40, max_wall_seconds: 420}
